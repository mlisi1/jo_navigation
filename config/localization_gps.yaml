
ekf_filter_node_odom:
  ros__parameters:
    frequency: 30.0
    two_d_mode: true
    publish_acceleration: true
    publish_tf: true

    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom

    debug: true

    odom0: glim_ros/odom_corrected
    odom0_config: [true, true, true,   # x, y, z — don't fuse absolute position
                   true, true, true,   # roll, pitch, yaw
                   true,  false, false,   # vx only (tracked vehicle, no lateral)
                   false, false, true,    # vyaw
                   false, false, false]   # accelerations
    odom0_relative: false
    odom0_differential: false


    odom1: visodom/odom
    odom1_config: [true, true, true,   # x, y, z — don't fuse absolute position
                   false, false, false,   # roll, pitch, yaw
                   true,  false, false,   # vx only (tracked vehicle, no lateral)
                   false, false, false,    # vyaw
                   false, false, false]   # accelerations
    odom1_relative: false
    odom1_differential: false
    

    imu0: imu/data
    imu0_config: [false, false, false,
                  false, false, true,   # don't fuse orientation, GLIM handles that
                  true, true, false,
                  false, false, true,    # fuse only vyaw from IMU
                  false, false, false]
    imu0_differential: false
    imu0_relative: false
    imu0_remove_gravitational_acceleration: true
    imu0_queue_size: 10



ekf_filter_node_map:
  ros__parameters:
    frequency: 15.0
    two_d_mode: true  # Recommended to use 2d mode for nav2 in mostly planar environments
    print_diagnostics: true
    debug: true
    publish_tf: true

    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: map

    odom0: odometry/local
    odom0_config: [false, false, false,   # x, y, z — don't fuse absolute position
                   false, false, true,   # roll, pitch, yaw
                   false, false, false,   # vx only (tracked vehicle, no lateral)
                   false, false, true,    # vyaw
                   false, false, false]   # accelerations
    odom0_relative: false
    odom0_differential: false
  

    odom1: odometry/gps
    odom1_config: [true,  true,  false,
                  false, false, false,
                  false, false, false,
                  false, false, false,
                  false, false, false]
    odom1_queue_size: 10
    odom1_differential: false
    odom1_relative: false





navsat_transform_node:
  ros__parameters:
    frequency: 4.0
    delay: 0.0
    zero_altitude: true
    magnetic_declination_radians: 0.0  # adjust for your location (Pisa ~0.04 rad)
    yaw_offset: 1.57                    # offset between IMU yaw and true north    zero_altitude: true                # flatten to 2D
    broadcast_cartesian_transform: true      # publishes utm→map transform
    broadcast_cartesian_transform_as_parent_frame: true
    publish_filtered_gps: true         # republishes GPS in filtered form
    use_odometry_yaw: false            # use IMU for heading, not GPS
    wait_for_datum: false              # compute datum from first fix
    # datum: [43.6851163, 10.4869685, 0.0]
    # use_local_cartesian: true
    

    